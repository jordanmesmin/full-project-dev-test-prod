pipeline:
  agent any
  stages:
    - stage('Build') {
        steps {
          script {
            echo 'Building application for Development...'
            // Commandes pour construire l'application
          }
        }
      }
    - stage('Test') {
        steps {
          script {
            echo 'Running tests for Development...'
            // Commandes pour exécuter les tests
          }
        }
      }
    - stage('Deploy') {
        steps {
          script {
            echo 'Deploying to Development...'
            // Commandes pour déployer l'application dans l'environnement de développement
          }
        }
      }
